<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width,initial-scale=1" />
        <title>Reset your password — E-shop</title>
        <style>
            /* Minimal, email-safe styles */
            body {
                margin: 0;
                padding: 24px;
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
                background: #f7f7f7;
                color: #111;
            }

            .card {
                max-width: 600px;
                margin: 0 auto;
                background: #fff;
                padding: 20px;
                border-radius: 6px;
                border: 1px solid #e6e6e6;
            }

            h1 {
                font-size: 18px;
                margin: 0 0 12px;
            }

            p {
                margin: 0 0 12px;
                font-size: 14px;
                line-height: 1.5;
            }

            .otp {
                display: inline-block;
                padding: 10px 16px;
                border-radius: 6px;
                background: #f3f4f6;
                border: 1px dashed #d1d5db;
                font-weight: 700;
                letter-spacing: 0.12em;
                font-size: 16px;
            }

            .muted {
                color: #6b7280;
                font-size: 12px;
            }

            .footer {
                margin-top: 16px;
                font-size: 12px;
                color: #6b7280;
                text-align: center;
            }

            @media (max-width:420px) {
                body {
                    padding: 12px
                }

                .card {
                    padding: 14px
                }
            }
        </style>
    </head>

    <body>
        <div class="card" role="article" aria-label="E-shop password reset email">
            <h1>E-shop — Password Reset</h1>

            <p>Hi <strong>
                    <%= name %>
                </strong>,</p>

            <p>You requested to reset your password. Please use the one-time password (OTP) below to proceed:</p>

            <p><span class="otp" aria-hidden="false">
                    <%= otp %>
                </span></p>

            <p class="muted">This code will expire in 5 minutes. Do not share it with anyone.</p>

            <p style="margin-top:12px">If you did not request this password reset, you can safely ignore this email.</p>

            <p style="margin-top:14px">Best,<br /><strong>E-shop Support</strong></p>

            <div class="footer">
                This is an automated message — please do not reply.
            </div>
        </div>
    </body>

</html>
